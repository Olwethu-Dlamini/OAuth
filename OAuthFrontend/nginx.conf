server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html =404;
    }

    # Optional: gzip, headers, etc. for production
    gzip on;
    gzip_types text/plain application/xml text/css application/javascript;
}